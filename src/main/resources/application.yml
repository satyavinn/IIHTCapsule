spring:
  datasource:
    url: jdbc:mysql://localhost:9999/taskmanager?autoReconnect=true
    username: root
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none

server:
  port: 8081





